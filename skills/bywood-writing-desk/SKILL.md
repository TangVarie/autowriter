---
name: bywood-writing-desk
description: 帆谷/BYWOOD 小红书种草文案的【写作台协议】。凡是给某个项目批量写小红书种草稿、改稿、写评论、处理写稿反馈、记规矩、改规矩、入库、导出，都走这套流程。它接的是 deskcore MCP 服务——项目的硬约束、个人调校笔记、正反案例、跨批次去重台账、真实爆款经验卡都在那里。当用户说"给X品写20条种草""这个项目怎么写""按这个方向出一批""这批重了，重写""以后都不要这么写""规则更新""我改了你看下""帮我标个正例""这个项目我们定过什么规则""给X品建个项目""列一下我的项目""导出""入库"时使用。⚠️ 它专治两件事：模型忘掉之前定过的规则，以及跨批次越写越像。不负责通用中文写作的去 AI 味——那个交给 human-writing；不负责提示词本身的迭代优化——那个交给 seeding-prompt-refiner。
---

# 写作台协议（引线）

这个文件**只做一件事**：把你带到 deskcore。协议正文不在这里，在服务端。

## 第一步，永远是 `get_protocol`

进入这个流程后，**先调 deskcore 的 `get_protocol`**，不带参数。它返回本次要遵守的完整协议：什么时候调哪个工具、报错怎么办、反馈怎么记、评论怎么处理、哪些事绝对不能做。

拿到之后**照着返回的 `protocol` 逐条执行**，它比你记忆里的任何版本都新。

## 三条在拿到协议之前就生效的规矩

1. **`get_protocol` 报错就停下来。** 告诉用户"写作台协议读不到"，不要凭记忆补一份继续写。
2. **不读、不写任何本地文件。** 用户没给稿子让你写、也没让你导出，就不该有文件操作。想找"上次那批稿子"或"交付文档"的念头出现时，先问自己该调哪个工具。
3. **运营不做选择题。** 分类、定 severity、定 scope 都是你的活。真分不清就问一个能用"是/否"回答的问题。

## 为什么协议放在服务端

本地 skill 拷出去之后改了没人提醒，而这份协议管的是流程纪律，过期了模型会按老规矩写而没人发现。放服务端意味着仓库一合并、部署一完成，所有人同时换版，运营不需要更新任何东西。

## 分工

- `human-writing` 管这一篇读起来像不像人写的
- `bywood-writing-desk`（本 skill）管这一批守不守项目规则、跟历史重不重
- `seeding-prompt-refiner` 管提示词本身怎么迭代
